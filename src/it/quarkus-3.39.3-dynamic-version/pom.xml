<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.gradle</groupId>
    <artifactId>quarkus-test-dynamic-version</artifactId>
    <!-- A version carrying a commit id: every build is a different version -->
    <version>1.0-${sha1}</version>

    <properties>
        <!-- quarkus.native.sources-only requires Quarkus 3.9.0 or above -->
        <quarkus.version>3.39.3</quarkus.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- The native image generation is cacheable on its own, the augmentation is cheap enough to always run -->
        <quarkus.native.enabled>true</quarkus.native.enabled>
        <quarkus.native.container-build>true</quarkus.native.container-build>
        <!-- the recipe: keep the moving version out of what the native image is keyed on -->
        <develocity.quarkus.version.independent.build>true</develocity.quarkus.version.independent.build>
        <quarkus.application.version>stable</quarkus.application.version>
        <!-- overridden per build, standing in for the commit id -->
        <sha1>unset</sha1>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.version}</version>
                <extensions>true</extensions>
                <executions>
                    <!-- Step 1: stop after the augmentation, leaving the runner jar and the
                         native-image arguments in target/native-sources -->
                    <execution>
                        <id>quarkus-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <configuration>
                            <systemProperties>
                                <quarkus.native.sources-only>true</quarkus.native.sources-only>
                            </systemProperties>
                        </configuration>
                    </execution>
                    <!-- Step 2: turn that jar into the native executable, keyed on it -->
                    <execution>
                        <id>quarkus-native-image</id>
                        <phase>package</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

</project>
